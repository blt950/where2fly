import * as sortable from 'sortable-tablesort/sortable.min.js';

window._ = _;
window.sortable = sortable;

// Note: This dependency throws export errors if you run `npm run dev`. Works fine in production build.